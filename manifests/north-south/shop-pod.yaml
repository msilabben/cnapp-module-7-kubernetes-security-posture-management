# ============================================================================
#  SHOP - the app this lab exposes, north-south instead of east-west
# ============================================================================
#  Same image as every other lab. Nothing insecure here on purpose, this
#  lab is about reachability, not container security.
#
#  kubectl apply -f manifests/north-south/shop-pod.yaml
# ============================================================================

apiVersion: v1
kind: Pod
metadata:
  name: shop
  labels:
    app: shop
spec:
  containers:
    - name: shop
      image: module7akslab967a44.azurecr.io/module-7-lab:1
      imagePullPolicy: Always
